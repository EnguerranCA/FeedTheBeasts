/**
 * Audio.js - Gestionnaire audio de base (ancien système)
 * Note: Ce module est conservé pour compatibilité mais le nouveau système
 * de sons est géré par Sound.js
 */

const Audio = {
    /**
     * Initialise le module audio
     */
    init: function() {
        console.log('[Audio] Module audio initialisé (legacy)');
        // Ce module est maintenant remplacé par Sound.js
        // Conservé pour compatibilité avec le code existant
    }
};

export { Audio };
